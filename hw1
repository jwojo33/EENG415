# EENG 415
# HW1
# Josh Wojciechowicz
import random
import matplotlib.pyplot as plt
import statistics as stat

data = random.sample(range(1, 1000), 200)

plt.hist(data)
plt.show()


data.sort()

skew = (3*(stat.mean(data) - stat.median(data)))/stat.stdev(data)
print(skew)
